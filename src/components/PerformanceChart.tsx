// This component was removed — the performance chart was taken out of the dashboard, and nothing
// in the app imports this file anymore. It's left here as an inert placeholder (rather than
// requiring a manual file delete in GitHub Desktop) so the build stays green either way. Safe to
// delete this file entirely next time you're in here, or just leave it — it does nothing either way.
export {};
